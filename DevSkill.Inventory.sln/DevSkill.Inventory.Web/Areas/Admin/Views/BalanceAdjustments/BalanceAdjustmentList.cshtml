@using DevSkill.Inventory.Web.Areas.Admin.Models.BalanceAdjustmentModels
@model BalanceAdjustmentListModel

@{
    ViewData["Title"] = "Balance Adjustment List";
}

@section Styles
{
    <link rel="stylesheet" href="https://cdn.datatables.net/2.2.2/css/dataTables.dataTables.min.css" />
    <link rel="stylesheet" href="https://cdn.datatables.net/buttons/3.0.2/css/buttons.dataTables.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
    <style>
        .dt-button {
            background-color: #007bff !important;
            color: white !important;
            border: 1px solid #007bff !important;
            transition: all 0.3s ease;
        }

        .dt-button:hover {
            background-color: white !important;
            color: #007bff !important;
        }
    </style>
}

<div class="app-content-header">
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-6">
                <h3 class="mb-0">Balance Adjustment List</h3>
            </div>
            <div class="col-sm-6 text-end">
                <ol class="breadcrumb float-sm-end mb-2">
                    <li class="breadcrumb-item"><a href="#">Home</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Balance Adjustment</li>
                </ol>
            </div>
        </div>

        <div class="row">
            <div class="col-sm-12 text-end mt-2">
                <button type="button" class="btn btn-primary btn-sm" data-bs-toggle="modal" data-bs-target="#modal-new-balance-adjustment">
                    <i class="fas fa-plus"></i> New Balance Adjustment
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Modals -->
<partial name="_ResponsePartial" />
<partial name="_ModalPartial" />

<div id="modal-edit-balance-adjustment" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="modalEditBalanceAdjustment" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content" id="editBalanceAdjustmentModalBody">
        </div>
    </div>
</div>

<div class="app-content">
    <div class="container-fluid">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title"><i class="fas fa-search mr-1"></i>Search Balance Adjustments</h3>
                    <div class="card-tools">
                        <button type="button" class="btn btn-tool" data-card-widget="collapse">
                            <i class="fas fa-plus"></i>
                        </button>
                    </div>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-sm-3">
                            <div class="form-group">
                                <label>Adjustment Type</label>
                                <input type="text" asp-for="SearchItem.AdjustmentType" class="form-control" id="SearchItem_AdjustmentType" />
                            </div>
                        </div>
                        <div class="col-sm-3">
                            <div class="form-group">
                                <label>Account Type</label>
                                <input type="text" asp-for="SearchItem.AccountType" class="form-control" id="SearchItem_AccountType" />
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card-footer">
                    <button id="searchButton" type="button" class="btn btn-primary float-end">Search</button>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header"><h3 class="card-title">Available Balance Adjustments</h3></div>
                    <div class="card-body">
                        <table id="balanceAdjustments" class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>#SL</th>
                                    <th>Date</th>
                                    <th>Adjustment Type</th>
                                    <th>Amount</th>
                                    <th>Note</th>
                                    <th>Account Type</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody></tbody>
                            <tfoot>
                                <tr>
                                    <th>#SL</th>
                                    <th>Date</th>
                                    <th>Adjustment Type</th>
                                    <th>Amount</th>
                                    <th>Note</th>
                                    <th>Account Type</th>
                                    <th>Actions</th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts
{
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="https://cdn.datatables.net/2.2.2/js/dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/buttons/3.0.2/js/dataTables.buttons.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/pdfmake.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/vfs_fonts.js"></script>
    <script src="https://cdn.datatables.net/buttons/3.0.2/js/buttons.html5.min.js"></script>
    <script src="https://cdn.datatables.net/buttons/3.0.2/js/buttons.print.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        $(function () {
            var table = $("#balanceAdjustments").DataTable({
                processing: true,
                serverSide: true,
                responsive: true,
                lengthChange: true,
                autoWidth: false,
                lengthMenu: [[10, 25, 50, -1], [10, 25, 50, 'All']],
                layout: {
                    topStart: 'buttons',
                    topEnd: 'search'
                },
                buttons: [
                    { extend: 'copy', text: '<i class="fas fa-copy"></i> Copy' },
                    { extend: 'excel', text: '<i class="fas fa-file-excel"></i> Excel' },
                    { extend: 'csv', text: '<i class="fas fa-file-csv"></i> CSV' },
                    { extend: 'pdf', text: '<i class="fas fa-file-pdf"></i> PDF' },
                    { extend: 'print', text: '<i class="fas fa-print"></i> Print' }
                ],
                ajax: {
                    url: "/Admin/BalanceAdjustments/GetBalanceAdjustmentsJsonData",
                    type: "POST",
                    contentType: "application/json",
                    dataType: "json",
                    data: function (d) {
                        d.SearchItem = {
                            AdjustmentType: $("#SearchItem_AdjustmentType").val(),
                            AccountType: $("#SearchItem_AccountType").val()
                        };
                        return JSON.stringify(d);
                    }
                },
                columnDefs: [
                    {
                        targets: 0,
                        render: function (data, type, row, meta) {
                            return meta.row + meta.settings._iDisplayStart + 1;
                        }
                    },
                    {
                        targets: 6,
                        orderable: false,
                        render: function (data, type, row) {
                            return `
                                <button class='btn btn-info btn-sm show-edit-modal' data-id='${data}' title='Edit'>
                                    <i class='fas fa-edit'></i>
                                </button>
                                <button class='btn btn-danger btn-sm show-bs-modal' title='Delete' data-id='${data}'>
                                    <i class='fas fa-trash'></i>
                                </button>
                            `;
                        }
                    }
                ]
            });

            $('#balanceAdjustments').on('click', '.show-bs-modal', function () {
                let id = $(this).data("id");
                let deleteModal = $("#modal-delete");
                deleteModal.find('.modal-body p').text('Are you sure you want to delete this balance adjustment?');
                $('#delete-id').val(id);
                $('#delete-form').attr('action', '/admin/balanceadjustments/delete');
                deleteModal.modal('show');
            });

            $('#balanceAdjustments').on('click', '.show-edit-modal', function () {
                let id = $(this).data("id");
                $.ajax({
                    url: `/Admin/BalanceAdjustments/Edit/${id}`,
                    type: 'GET',
                    success: function (response) {
                        $('#editBalanceAdjustmentModalBody').html(response);
                        $('#modal-edit-balance-adjustment').modal('show');
                    },
                    error: function () {
                        alert('Failed to load edit form.');
                    }
                });
            });

            $("#searchButton").click(function () {
                $('#balanceAdjustments').DataTable().ajax.reload(null, false);
            });
        });
    </script>
}
